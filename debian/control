Source: git-ftp
Section: misc
Priority: optional
Maintainer: René Moser <mail@renemoser.net>
Build-Depends: debhelper (>= 7.0.50~), docbook-xml, docbook-xsl, xsltproc
Standards-Version: 3.8.4
Vcs-Browser: http://github.com/resmo/git-ftp
Vcs-Git: git://github.com/resmo/git-ftp.git
Homepage: http://github.com/resmo/git-ftp

Package: git-ftp
Architecture: all
Depends: ${misc:Depends}, curl (>= 7.15.5), git-core (>= 1.6), grep (>= 2.5.1)
Description: Uploads the git tracked files to a FTP server
 git-ftp is a shell script for uploading git tracked files to a FTP server.
 By default, it uploads only those files which have changed since the last
 upload.
 This saves time and bandwith. You can even play with different branches. See
 manpage for more options.
